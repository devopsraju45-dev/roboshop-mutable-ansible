- name: SetUp NodeJS repos
  ansible.builtin.shell: curl --silent --location https://rpm.nodesource.com/setup_16.x | bash

- name: Install NodeJs
  ansible.builtin.yum:
    name: nodejs
    state: installed

- name: Add roboshop user
  ansible.builtin.user:
    name: roboshop
    comment: Roboshop App User

- name: Remove old app content
  ansible.builtin.file:
    path: /home/roboshop/{{COMPONENT}}
    state: absent

- name: Create app directory
  ansible.builtin.file:
    path: /home/roboshop/{{COMPONENT}}
    state: directory
    owner: roboshop
    group: roboshop
    mode: '0755'

- name: Download {{COMPONENT}} content
  ansible.builtin.get_url:
    url: https://github.com/roboshop-devops-project/{{COMPONENT}}/archive/main.zip
    dest: /tmp/{{COMPONENT}}.zip

- name: Extract {{COMPONENT}} content to temp directory
  ansible.builtin.unarchive:
    src: /tmp/{{COMPONENT}}.zip
    dest: /tmp/{{COMPONENT}}
    remote_src: yes

- name: Copy extracted content to app directory
  ansible.builtin.copy:
    src: /tmp/{{COMPONENT}}/{{COMPONENT}}-main/
    dest: /home/roboshop/{{COMPONENT}}/
    owner: roboshop
    group: roboshop
    mode: '0755'
    remote_src: yes

- name: Install nodejs dependencies
  community.general.npm:
    path: /home/roboshop/{{COMPONENT}}
  become: yes
  become_user: roboshop

- name: Setup {{COMPONENT}} systemd service
  ansible.builtin.template:
    src: systemd.service
    dest: /etc/systemd/system/{{COMPONENT}}.service
    mode: '0644'

- name: Reload systemd daemon and start service
  ansible.builtin.systemd:
    name: "{{COMPONENT}}"
    state: restarted
    enabled: yes
    daemon_reload: yes
